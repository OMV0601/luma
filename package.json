{
  "name": "foolproof",
  "version": "0.1.0",
  "private": true,
  "description": "FoolProof — train your financial defenses by negotiating against AI scammers.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm run dev -w server\" \"npm run dev -w client\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
